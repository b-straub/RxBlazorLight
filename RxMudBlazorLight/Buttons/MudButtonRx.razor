@inherits MudButton

@RenderBase()

@code {
    [Parameter]
    public Func<Task<bool>>? ConfirmExecution { get; set; }

    [Parameter]
    public Action? BeforeExecution { get; set; }

    [Parameter]
    public Action? AfterExecution { get; set; }

    [Parameter]
    public ICommand? RxCommand { get; set; }

    private RenderFragment RenderBase() => builder => base.BuildRenderTree(builder);
    private ButtonRX? _buttonRX;

    protected override void OnInitialized()
    {
        _buttonRX = ButtonRX.Create(MBButtonType.DEFAULT, RxCommand, ConfirmExecution, BeforeExecution, AfterExecution);
        OnClick = _buttonRX.OnClick;

        base.OnInitialized();
    }

    protected override void OnParametersSet()
    {
        ArgumentNullException.ThrowIfNull(RxCommand);
        Disabled = !RxCommand.CanExecute();
        base.OnParametersSet();
    }
}
