@inherits MudRating

@RenderBase()

@code {
    [Parameter, EditorRequired]
    public required IState<int> State { get; init; }

    [Parameter]
    public Func<int, bool>? CanChangeCallback { get; init; }

    private RenderFragment RenderBase() => builder => base.BuildRenderTree(builder);

    protected override void OnInitialized()
    {
        SelectedValueChanged = EventCallback.Factory.Create<int>(this, v =>
        {
            State.Value = v;
        });

        base.OnInitialized();
    }

    protected override void OnParametersSet()
    {
        Disabled = (CanChangeCallback is not null && !CanChangeCallback(State.Value));
        SelectedValue = State.Value;

        base.OnParametersSet();
    }
}
