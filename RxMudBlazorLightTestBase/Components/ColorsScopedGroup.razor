
<MudSelectAsyncRx StateGroupAsync=@Scope.TestColors SelectionChangedAsync=@TestService.ColorsStateScope.ChangeTestColorAsync(Context)
                  CanChange=@TestService.ColorsStateScope.CanChangeTestColor(Context) />

@code {
    [CascadingParameter]
    public required TestService.ColorsStateScope Scope { get; init; }

    [Parameter]
    public required int Context { get; init; }
}
