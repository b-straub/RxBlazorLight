
<MudSelectAsyncRx StateGroupAsync=@Scope.TestColors ChangingAsyncCallback=@Scope.ChangeTestColorAsync(Context)
                  CanChangeCallback=@TestService.ColorsStateScope.CanChangeTestColor(Context) />

@code {
    [CascadingParameter]
    public required TestService.ColorsStateScope Scope { get; init; }

    [Parameter]
    public required int Context { get; init; }
}
